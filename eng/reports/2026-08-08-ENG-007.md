# ENG-007 — Landing trust & credibility — 2026-08-08

**Status**: DONE — shipped and verified  
**Blast**: GREEN (landing.py only, no migration, no HAL brain)  
**Builder**: general-purpose Claude subagent (Codex CLI not available on this machine)  
**Reviewer**: general-purpose Claude subagent — APPROVED

---

## What shipped

Three additions to `services/hal-orchestrator/hal_orchestrator/routes/landing.py`:

1. **Hero chips** changed to trust signals: "No account" / "No password" / "No app to install"
2. **`.hero-trust` badge row** in the hero below chips — two checkmarked lines: "Your number is never sold or shared" and "Delete everything with one text"
3. **"How it works" 3-step section** (`id="how"`) immediately after the hero — three numbered steps covering the core loop (Add HAL → text in → ask back)

All copy is factually accurate. No invented counts. No fabricated testimonials. Social proof hypothesis deferred — awaiting a real user quote from Adnan.

## Tests

New section `8c. trust & credibility (ENG-007)` in `tests_onboarding_parent.py`:
- trust chips replace old feature chips
- hero-trust div present
- trust badge: number never sold
- trust badge: delete with one text
- how-steps section present
- how-steps has all three step items
- how-steps h3 headings present

All 7 new assertions pass. `tests_onboarding_parent.py` and `tests_admin_dash.py` — both fully green.

## Production verification

`python3 eng/scripts/verify_prod.py` → all checks passed:
- health 200
- landing 200
- landing is no-store (A/B arms uncacheable)
- ios gets &body=
- android gets ?body=
- sms prefill carries the parent-track trigger
- hero CTA present above the fold
- sticky mobile CTA present
- both hero-CTA arms served
- tap beacon accepts events
- arms seen: ['a', 'b']

## A/B arm integrity

No new arm introduced. Both "a" and "b" arms get identical trust content. Historical data is clean.

## Outstanding

- **Social proof**: deferred — Adnan must supply a real user testimonial to implement hypothesis 1.
- **`railway.toml` cleanup**: root copy wasn't removed (rm permission denied). Adnan: run `rm railway.toml`.
- **Growth action**: re-enable Google campaign to $15/day and monitor mobile tap rate vs. 150-view / ≥2% gate.

## Uncommitted files

- `services/hal-orchestrator/hal_orchestrator/routes/landing.py`
- `services/hal-orchestrator/tests_onboarding_parent.py`
- `eng/queue/ENG-007-landing-trust-credibility.md`
- `eng/reports/2026-08-08-ENG-007.md`
