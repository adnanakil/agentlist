{
  "name": "hal-whatsapp-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "HAL WhatsApp bridge — links HAL's WhatsApp account (Baileys companion device) to the hal-orchestrator /api/message contract",
  "main": "bridge.js",
  "scripts": {
    "start": "node bridge.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@whiskeysockets/baileys": "6.7.23",
    "pino": "^9.6.0",
    "qrcode-terminal": "^0.12.0"
  }
}
